import { Badge as BadgeComponent } from "./badge";

const exportContent = {
  title: "Atoms/Badge",
  component: BadgeComponent,
  tags: ["autodocs"],
};

export const Badge = {};

export default exportContent;
